The Controller namespace deals with interactive functionalities such as 
event handling in the window. The controller recieves these inputs and dispatches
them as instructions and modifications on the Model component. Simueltaneously, the
dispatcher from the controller might instruct the View component to update visuals.

Common Class(es) include:

- Client
- Dispatcher