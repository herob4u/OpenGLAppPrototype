The Model namespace will contain definitions that represent data of objects in
the application. This can include description of 3D models, images, etc.

The model is modified by the controller component. The representation of 
the model is then created via the View component.

Common class(es) include:

- Transforms
- Model/Mesh
- Texture
- HUDElement